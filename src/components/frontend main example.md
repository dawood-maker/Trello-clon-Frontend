Divide dashboard into smaller reusable components.
Read and explain about this.
```
const res = await axios.post(
        "http://localhost:5001/api/auth/login",
        { email, password },
        { withCredentials: true },
      );
```



.